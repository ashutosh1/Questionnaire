<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Questionnaire" %></title>
    <%= csrf_meta_tags %>
    <!--[if lt IE 9]>
     <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->
    <%= stylesheet_link_tag :application, :media => "all" %>
    <%= javascript_include_tag :application %>
    <%= csrf_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  </head>

  <body>
    <% if user_signed_in? %>
      <%= render partial: "layouts/nav_bar" %>
    <% end %>
    
    <div class="flashMessageDiv">
      <%= render partial: "layouts/flash_message" %> 
    </div>

    <div class="span9">
      <% if user_signed_in? %>
        <%= yield %>
      <% else %>
        <div id="signInLink"><%= link_to "Sign in with vinsol account", user_omniauth_authorize_path(:google_oauth2) %></div>
      <% end %>
    </div>
    
  </body>
</html>
