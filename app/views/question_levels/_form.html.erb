<%= form_for @question_level, :html => { :class => 'form-horizontal' } do |f| %>
  <%= render :partial => "shared/errors", :locals => { :obj => @question_level } %>  
  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %><span class="requiredField"><%= mark_required(@question_level, 'name') %></span>
    <div class="controls">
      <%= f.text_field :name, :class => 'text_field' %>&nbsp;&nbsp;
      <%= f.submit nil, :class => 'btn btn-primary' %>
    </div>
  </div>    
<% end %>